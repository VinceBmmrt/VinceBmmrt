if (valeurIsLabel && valeur ==="Oui"){
    return (<Badge text="Oui" type="success" hasIcon />)
} 

if(valeurIsLabel && valeur ==="Non" ) { return  (<LabelGreyCustomCrossIcon text="Non" />)
}

if (!valeurIsLabel && valeurIsNotNumber) {
    return
   ( <b> {valeur}</b>)
} 
if (!valeurIsNotNumber){
    return(
       <> <div className="flex items-center whitespace-nowrap sm:ml-1">
    <span className="font-bold text-end"></>
    )}


        if (Number(valeur) === 0) { 
            return (<span className="mr-2">{valeur}</span>)
        } if 
        (Number(valeur) !=== 0)
         { return 
        
         <TagData
                valeur={`${formatNumberWithSpace(Number(valeur))} ${
                   
                   if (unite = true  ){
                    return ("" + unite)
                   } if (!unite) {
                    return ("")
                
                    
                }`
                percentage={percentageEvolution}
            />
        </span>
    </div>} 

    












   


   